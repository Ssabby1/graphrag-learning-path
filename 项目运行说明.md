# 基于知识图谱的个性化学习路径推荐系统运行说明

## 0. 跨平台公开样例（推荐）

macOS / Linux 新用户只需 Python 3.11–3.13 和 Node.js 18+，不需要复用 Windows 虚拟环境，也不需要安装 Java 或 Neo4j：

```bash
./setup.sh
./start-dev.sh
```

该模式通过 `GRAPH_BACKEND=csv` 使用 `data/seed/` 下 15 个双语合成知识点，完整运行路径规划、关系证据检索、Evidence Pack、回答生成和 citation 校验。使用 `./stop-dev.sh` 停止服务。

以下 PowerShell + Neo4j 流程用于 Windows 和完整本地图谱。

## 1. 项目内容

本项目实现了一个面向数字逻辑课程的个性化学习路径推荐系统，主要包含：

- `frontend/`：Vue 3 前端页面，用于选择目标知识点、输入已掌握知识点、展示推荐路径和解释结果。
- `backend/`：FastAPI 后端服务，提供知识图谱查询、先修路径推荐、GraphRAG 问答和解释接口。
- `neo4j/`：Neo4j Community 本地运行目录。压缩包中保留运行程序，不包含本机私有数据库状态。
- `章节数据/数据汇总/outputs/fixed/`：整理后的知识点与关系 CSV 数据，用于重新导入 Neo4j。
- `scripts/`：数据导入、图谱质量校验等脚本。
- `docs/`：项目综述、系统设计、接口说明、实验结果等文档。
- `成果材料/`：论文、答辩 PPT、演示稿和关键图片等成果文件。

## 2. 需要提前安装的软件

完整本地图谱建议在 Windows 10/11 环境中运行。安装完成后请重新打开 PowerShell，使环境变量生效。

1. 安装 Python 3.11 或 3.12
   - 下载地址：https://www.python.org/downloads/
   - 安装时勾选 `Add python.exe to PATH`。
   - 验证命令：

```powershell
python --version
```

2. 安装 Node.js 18 或更高版本
   - 下载地址：https://nodejs.org/
   - 建议安装 LTS 版本。
   - 验证命令：

```powershell
node --version
npm --version
```

3. 安装 JDK 21 或更高版本
   - Neo4j 2025.x 需要 Java 21+。
   - 可安装 Eclipse Temurin JDK：https://adoptium.net/temurin/releases/
   - 验证命令：

```powershell
java -version
```

4. PowerShell 脚本执行权限
   - 如果运行 `.ps1` 文件时提示脚本被禁止执行，在 PowerShell 中执行：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

## 3. 第一次运行

在项目根目录打开 PowerShell，执行一次初始化脚本：

```powershell
.\setup.ps1
```

该脚本会自动完成以下操作：

- 生成 `backend/.env` 和 `frontend/.env`。
- 创建 `backend/.venv` Python 虚拟环境。
- 安装后端依赖。
- 安装前端依赖。
- 初始化 Neo4j 密码。
- 启动 Neo4j，导入知识图谱数据，然后关闭 Neo4j。

默认 Neo4j 本地密码为：

```text
kg_learning_path_2026
```

如需自定义密码，可以这样运行：

```powershell
.\setup.ps1 -Neo4jPassword "your_password_here"
```

自定义密码建议在第一次初始化时指定。Neo4j 初始化完成后再次执行 `setup.ps1` 时，脚本会沿用 `backend/.env` 中已有的密码配置。

初始化完成后，后续通常不需要再次执行 `setup.ps1`。

## 4. 启动系统

在项目根目录执行：

```powershell
.\start-dev.ps1
```

脚本会依次启动：

- Neo4j 图数据库
- FastAPI 后端
- Vue 前端开发服务器

启动后访问：

- 前端页面：http://127.0.0.1:5173
- 后端接口文档：http://127.0.0.1:8000/docs
- Neo4j 浏览器：http://127.0.0.1:7474

Neo4j 浏览器登录信息：

```text
用户名：neo4j
密码：kg_learning_path_2026
```

如果初始化时自定义了密码，请使用自定义密码。

## 5. 停止系统

在项目根目录执行：

```powershell
.\stop-dev.ps1
```

该脚本会关闭 8000、5173 端口上的后端/前端进程，并停止 Neo4j。

## 6. 手动运行方式

如果一键脚本无法使用，可以按以下步骤手动运行。

### 6.1 后端依赖安装

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
```

打开 `backend/.env`，设置：

```env
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=kg_learning_path_2026
NEO4J_DATABASE=neo4j
LLM_ENABLED=false
```

### 6.2 前端依赖安装

```powershell
cd frontend
npm install
Copy-Item .env.example .env
```

`frontend/.env` 中默认地址如下：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
```

### 6.3 初始化 Neo4j

```powershell
cd neo4j\neo4j-community-2025.12.1
.\bin\neo4j-admin.bat dbms set-initial-password kg_learning_path_2026
.\bin\neo4j.bat start
```

回到项目根目录，导入图谱数据：

```powershell
cd ..\..
$env:NEO4J_PASSWORD="kg_learning_path_2026"
.\backend\.venv\Scripts\python.exe scripts\import_data.py --clear-target
```

### 6.4 启动后端

```powershell
cd backend
$env:PYTHONPATH="."
.\.venv\Scripts\python.exe run.py
```

### 6.5 启动前端

另开一个 PowerShell：

```powershell
cd frontend
npm run dev
```

## 7. 可选的大模型解释配置

系统默认使用本地模板解释，不需要联网，也不需要 API Key。

如需启用远程大模型解释，可以在 `backend/.env` 中配置：

```env
LLM_ENABLED=true
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

不要把真实 API Key 提交到公开仓库或发送给无关人员。

## 8. 测试与校验

### macOS / Linux 隔离环境

仓库中可能保留过 Windows 创建的 `backend/.venv`，该环境不能在 macOS/Linux 复用，也不要覆盖。建议单独创建平台环境：

```bash
python3 -m venv backend/.venv-macos
backend/.venv-macos/bin/python -m pip install -r backend/requirements.txt
cd backend
PYTHONPATH=. .venv-macos/bin/python -m pytest
```

回到仓库根目录后，可以不启动 Neo4j，直接用 CSV 完成全量 DAG、最长路径、深度分布和数据哈希校验：

```bash
backend/.venv-macos/bin/python scripts/validate_graph.py \
  --concepts-csv "章节数据/数据汇总/outputs/fixed/concepts_all.csv" \
  --relations-csv "章节数据/数据汇总/outputs/fixed/relations_all.csv" \
  --report backend/docs/graph_validation_report_stage1.md \
  --json-report backend/docs/graph_validation_report_stage1.json
```

前端使用 lockfile 重建当前平台依赖，不要直接复用 Windows 的 `node_modules`：

```bash
cd frontend
npm ci
npm run build
```

Windows 原有 PowerShell 流程保持不变。

后端测试：

```powershell
cd backend
.\.venv\Scripts\python.exe -m pytest
```

前端构建：

```powershell
cd frontend
npm run build
```

图谱质量校验：

```powershell
$env:NEO4J_PASSWORD="kg_learning_path_2026"
.\backend\.venv\Scripts\python.exe scripts\validate_graph.py --report backend\docs\graph_validation_report_stage1.md --json-report backend\docs\graph_validation_report_stage1.json
```

## 9. 常见问题

### 9.1 `python` 命令找不到

重新安装 Python，并确认安装时勾选 `Add python.exe to PATH`。也可以尝试：

```powershell
py -3 --version
```

### 9.2 `npm` 命令找不到

重新安装 Node.js LTS，并重新打开 PowerShell。

### 9.3 Neo4j 启动失败

优先检查：

- `java -version` 是否为 21 或更高版本。
- 7474、7687 端口是否被其他 Neo4j 或数据库进程占用。
- `backend/.env` 中的 `NEO4J_PASSWORD` 是否与 Neo4j 初始化密码一致。

### 9.4 后端接口返回 `Neo4j unavailable`

通常是 Neo4j 未启动或密码不一致。可以先执行：

```powershell
.\stop-dev.ps1
.\start-dev.ps1
```

如果仍然失败，检查 `backend/.env` 中的 Neo4j 配置。

### 9.5 前端页面打开但没有数据

检查后端接口文档是否能打开：

```text
http://127.0.0.1:8000/docs
```

如果后端正常，再检查 `frontend/.env` 中的 `VITE_API_BASE_URL` 是否为：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
```

## 10. 推荐查看顺序

1. 阅读 `README.md` 了解项目整体结构。
2. 按本说明完成初始化和启动。
3. 打开前端页面体验推荐流程。
4. 打开 `http://127.0.0.1:8000/docs` 查看接口。
5. 阅读 `docs/` 中的系统设计、接口说明与实验结果。
6. 如需复用论文材料，查看 `成果材料/`。
